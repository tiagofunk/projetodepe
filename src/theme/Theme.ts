import colors from "theme/colors.json";
import fonts from "theme/fonts.json";
import spaces from "theme/spaces.json";
import breakPoints from "theme/breakPoints.json";

const Theme = {
  colors,
  fonts,
  spaces,
  breakPoints
};

export default Theme;
